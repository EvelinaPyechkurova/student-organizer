# Django secret key — generate a real one for production
SECRET_KEY=

# Debug mode — set to False in production
DEBUG=True

# Database configuration
DATABASE_ENGINE=django.db.backends.postgresql
DATABASE_NAME=student_organizer
DATABASE_USER=django_organizer_user
DATABASE_PASSWORD=
DATABASE_HOST=db # 'db' is the Docker service name; use 'localhost' if running without Docker
DATABASE_PORT=5432